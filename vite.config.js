import { defineConfig } from 'vite';

export default defineConfig({
  base: '/https://github.com/Rishabh001pawar/Portfolio-Website/',  // GitHub repo name with leading and trailing slashes
  build: {
    outDir: 'dist',
  }
});
